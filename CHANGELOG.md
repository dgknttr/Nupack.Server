# Changelog

Notable user-facing changes will be recorded here once the project starts publishing tagged `0.x` releases.

## Unreleased

### Changed

- Removed obsolete manual test scripts and unused sample projects.
- Kept the CLI restore consumer as an explicit fixture under `tests/Fixtures`.
- Replaced stale test documentation with commands and behavior that match the current suite.
- Documented the current product needs and release-gated roadmap.
- Removed the API-hosted `/ui` and `/frontend` demos; use the separate Web application on port `5004`.

The Git history is the source of truth for work completed before the first tagged release.
